To create an image in the current directory, run:

    $ ./setup-vmx

This will create a 32 MiB image stored in `real-vmx.img'. Read the script
to get a hint on how to rebuild Real VMX and put a new kernel in the image.
